
  # Design historical justice website

  This is a code bundle for Design historical justice website. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
